# Payload Audit Report (ตรวจสอบโครงสร้างข้อมูล)

ตารางตรวจสอบความถูกต้องของข้อมูลที่ถูกส่งออกไปที่ RAM เปรียบเทียบกับเอกสารต้นแบบ

| หัวข้อในเอกสาร (Target) | หัวข้อจริงในบอท (RAM) | เทียบ 2 แห่ง | คำนวณจริง | ข้อมูลจริง (ใช้งานจริง) | มาตรฐานสากล (คำนวณได้ดี) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Header** | | | | | |
| `ID` | *ไม่มีใน RAM (ถูกสร้างตอนเซฟ)* | ตรงกัน (ตอนจบ) | ใช้โค๊ดสร้างจริง | ข้อมูลจริง (เวลา+ชื่อ) | มาตรฐานสากล |
| **meta.** | **meta.** | | | | |
| `timestamp` | `timestamp` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `symbol` | `symbol` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `session` | `session` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `price` | `price` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `data_age_ms` | `data_age_ms` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `data_quality` | `data_quality` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| **market_context.** | **market_context.** | | | | |
| `state` | `state` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | ใช้การคำนวณภายใน (ไม่ใช่สากล) |
| `description` | `description` | มีหัวข้อตรงกัน | ไม่มีการคำนวณจริง | ข้อมูลเดา (Hardcode ผูกกับ state) | ไม่ใช่มาตรฐานสากล |
| `volatility_regime` | `volatility_regime` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง (จาก ATR) | ใช้การคำนวณภายใน |
| `news_impact` | `news_impact` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `expected_volatility_%`| `expected_volatility_%`| มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง `(ATR/Close)` | ใช้สูตรดัดแปลงเอง |
| **timeframes.m1.** | **timeframes.m1.** | | | | |
| `last_candle` | `last_candle` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `ema5` | `ema5` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล (Pandas EWM) |
| `ema20` | `ema20` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล (Pandas EWM) |
| `rsi` | `rsi` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล (Wilder's) |
| `stoch_k` | `stoch_k` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `stoch_d` | `stoch_d` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `macd` | `macd` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `macd_signal` | `macd_signal` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| **timeframes.m5.** | **timeframes.m5.** | | | | |
| `bias` | `bias` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง (Close vs EMA) | ใช้สูตรดัดแปลงเอง |
| `ema5` | `ema5` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `ema10` | `ema10` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `ema20` | `ema20` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `ema50` | `ema50` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `bb_upper` | `bb_upper` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล (SMA20+2STD) |
| `bb_lower` | `bb_lower` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล (SMA20-2STD) |
| `bb_width` | `bb_width` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `rsi` | `rsi` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `stoch_k` | `stoch_k` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `stoch_d` | `stoch_d` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `macd` | `macd` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `macd_signal` | `macd_signal` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `adx` | `adx` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล (Wilder's ADX) |
| `atr` | `atr` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `support` | `support` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | ใช้สูตรดัดแปลงเอง (SR Engine) |
| `resistance` | `resistance` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | ใช้สูตรดัดแปลงเอง (SR Engine) |
| `pivot` | `pivot` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล `(H+L+C)/3` |
| **timeframes.m15.** | **timeframes.m15.** | | | | |
| `bias` | `bias` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | ใช้สูตรดัดแปลงเอง |
| **price_action.** | **price_action.** | | | | |
| `pattern` | `pattern` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | ใช้สูตรดัดแปลงเอง (Pattern Rec) |
| `last_candle_bias` | `last_candle_bias`| มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | ใช้สูตรดัดแปลงเอง |
| `body_strength` | `body_strength` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | ใช้สูตรดัดแปลงเอง |
| `wick_dominance` | `wick_dominance` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | ใช้สูตรดัดแปลงเอง |
| `momentum_bias` | `momentum_bias` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | ใช้สูตรดัดแปลงเอง |
| `move_quality` | `move_quality` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | ใช้สูตรดัดแปลงเอง |
| `trap_alert` | `trap_alert` | มีหัวข้อตรงกัน | ไม่มีการคำนวณจริง | ข้อมูลเดา (ถ้าพังจะยัดเป็น TRUE) | ไม่ใช่มาตรฐานสากล (มั่วค่า) |
| `sr_interaction` | `sr_interaction` | มีหัวข้อตรงกัน | ไม่มีการคำนวณจริง | ข้อมูลเดา (ถ้าพังจะยัด MID_RANGE)| ไม่ใช่มาตรฐานสากล (มั่วค่า) |
| **volume.** | **volume.** | | | | |
| `tick_volume` | `tick_volume` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล |
| `volume_momentum` | `volume_momentum` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | ใช้สูตรดัดแปลงเอง |
| `volume_vs_average`| `volume_vs_average`| มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | มาตรฐานสากล `(Vol/MA20)` |
| **analysis.** | **analysis.** | | | | |
| `trend_direction` | `trend_direction` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | ใช้สูตรดัดแปลงเอง (Trend Engine) |
| `trend_type` | `trend_type` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง | ใช้สูตรดัดแปลงเอง |
| `trend_strength_score`| `trend_strength_score`|มีหัวข้อตรงกัน| ไม่มีการคำนวณจริง | ข้อมูลเดา (ใช้เลข Slope คูณดื้อๆ) | ไม่ใช่มาตรฐานสากล |
| `mtf_alignment_%` | `mtf_alignment_%` | มีหัวข้อตรงกัน | ไม่มีการคำนวณจริง | ข้อมูลเดา (หาไม่เจอ ยัดค่า 50) | ไม่ใช่มาตรฐานสากล |
| `compression_quality_%`| `compression_quality_%`| มีหัวข้อตรงกัน| ไม่มีการคำนวณจริง | ข้อมูลเดา (หาไม่เจอ ยัดค่า 50) | ไม่ใช่มาตรฐานสากล |
| `exhaustion_risk_%`| `exhaustion_risk_%`| มีหัวข้อตรงกัน | ไม่มีการคำนวณจริง | ข้อมูลเดา (หาไม่เจอ ยัดค่า 30) | ไม่ใช่มาตรฐานสากล |
| `bos_detected` | `bos_detected` | มีหัวข้อตรงกัน | ไม่มีการคำนวณจริง | ข้อมูลเดา (ยัด False เสมอ) | ไม่ใช่มาตรฐานสากล |
| **signals.** | **signals.** | | | | |
| `triggered` | `triggered` | มีหัวข้อตรงกัน | ไม่มีการคำนวณจริง | ข้อมูลเดา (ปล่อยว่าง `[]`) | ไม่ใช่มาตรฐานสากล |
| `count` | `count` | มีหัวข้อตรงกัน | ไม่มีการคำนวณจริง | ข้อมูลเดา (ยัด `0`) | ไม่ใช่มาตรฐานสากล |
| `top_signal` | `top_signal` | มีหัวข้อตรงกัน | ไม่มีการคำนวณจริง | ข้อมูลเดา (ยัด `"NO"`) | ไม่ใช่มาตรฐานสากล |
| **decision_layer.**| **decision_layer.** | | | | |
| `tradeable` | `tradeable` | มีหัวข้อตรงกัน | ใช้โค๊ดคำนวณจริง | ข้อมูลจริง (เช็กจาก State) | ใช้การตัดสินใจภายใน |
| `tradeable_reason` | `tradeable_reason`| มีหัวข้อตรงกัน | ไม่มีการคำนวณจริง | ข้อมูลเดา (ยัดประโยคตายตัว) | ไม่ใช่มาตรฐานสากล |
| `confidence_score` | `confidence_score`| มีหัวข้อตรงกัน | ไม่มีการคำนวณจริง | ข้อมูลเดา (ดึง 50 มาใส่ดื้อๆ) | ไม่ใช่มาตรฐานสากล |
| `stability_score` | `stability_score` | มีหัวข้อตรงกัน | ไม่มีการคำนวณจริง | ข้อมูลเดา (ดึง 50 มาใส่ดื้อๆ) | ไม่ใช่มาตรฐานสากล |
| `quality_score` | `quality_score` | มีหัวข้อตรงกัน | ไม่มีการคำนวณจริง | ข้อมูลเดา (ดึง 50 มาใส่ดื้อๆ) | ไม่ใช่มาตรฐานสากล |
| `risk_level` | `risk_level` | มีหัวข้อตรงกัน | ไม่มีการคำนวณจริง | ข้อมูลเดา (ยัดค่า `"MEDIUM"`) | ไม่ใช่มาตรฐานสากล |
| `suggested_expiry_minutes`| `suggested_expiry_minutes`| มีหัวข้อตรงกัน| ไม่มีการคำนวณจริง| ข้อมูลเดา (ยัดค่า `5` เสมอ) | ไม่ใช่มาตรฐานสากล |
| `suggested_action` | `suggested_action`| มีหัวข้อตรงกัน | ไม่มีการคำนวณจริง | ข้อมูลเดา (ยัดค่า `"WAIT"`) | ไม่ใช่มาตรฐานสากล |
| `final_reason_th` | `final_reason_th` | มีหัวข้อตรงกัน | ไม่มีการคำนวณจริง | ข้อมูลเดา (ข้อความแจ้งตายตัว) | ไม่ใช่มาตรฐานสากล |
