# FINALBOT - ระบบเทรดอัตโนมัติสำหรับ IQ Option

## ภาพรวมโครงการ

FINALBOT เป็นระบบเทรดอัตโนมัติขั้นสูงสำหรับแพลตฟอร์ม IQ Option ที่พัฒนาด้วย Python และใช้ AI เพื่อวิเคราะห์ตลาดและตัดสินใจการซื้อขาย

## ฟีเจอร์หลัก

### 1. ระบบตรวจจับอะโนมัลี้ (Anomaly Detection)

ระบบตรวจจับอะโนมัลี้ใหม่ได้รับการเพิ่มเข้ามาเพื่อเพิ่มความปลอดภัยและคุณภาพของข้อมูลการซื้อขาย:

#### 1.1 Health Check IQ Option API
- **หน้าที่:** ตรวจสอบสุขภาพของการเชื่อมต่อกับ API ของ IQ Option
- **การทำงาน:** ตรวจสอบเวลาตอบสนองของ API
- **ระดับการเตือน:**
  - **NORMAL:** < 7 วินาที
  - **WARNING:** 7-10 วินาที
  - **CRITICAL:** > 10 วินาที
- **Zero Tolerance:** ไม่มีการ retry ตามมาตรฐานของระบบ

#### 1.2 Price Spike Detection
- **หน้าที่:** ตรวจจับการกระโดดราคาที่ผิดปกติ
- **พารามิเตอร์:**
  - `spike_threshold`: 0.3 (30%)
  - `spike_window`: 20 candle
- **การทำงาน:** วิเคราะห์การเปลี่ยนแปลงราคในหน้าต่างเวลาที่กำหนด

#### 1.3 Zero Volume Monitoring (สำหรับ OTC)
- **หน้าที่:** ตรวจจับ candle ที่มีปริมาณการซื้อขายเป็นศูนย์
- **การใช้งาน:** สำหรับตลาด OTC ที่อาจมีปัญหาในการซื้อขาย
- **การทำงาน:** ตรวจสอบว่า volume = 0 และบันทึกเป็นอะโนมัลี้

#### 1.4 Impossible Candle Detection
- **หน้าที่:** ตรวจจับ candle ที่เป็นไปไม่ได้ตามกฎของตลาด
- **ประเภทที่ตรวจจับ:**
  - Low > High
  - High < Low
  - Open > Close ในแนวลง
  - Close < Open ในแนวขึ้น
  - ราคาติดลบ

#### 1.5 Anomaly History Tracking
- **หน้าที่:** ติดตามประวัติของอะโนมัลี้
- **การบันทึก:** บันทึกลง CSV พร้อมรายละเอียด
- **การวิเคราะห์:** คำนวณจำนวนอะโนมัลี้ต่อช่วงเวลา

### 2. ระบบบันทึกข้อมูล (CSV Logging)

#### 2.1 รูปแบบการบันทึก
ไฟล์ CSV ถูกบันทึกในรูปแบบเดียวสำหรับทุกประเภทของอะโนมัลี้:
```
data_base/csv/iq_option/anomaly_logs/{symbol}_{anomaly_type}_{date}.csv
```

**ตัวอย่าง:**
- `EURUSD_spike_2026_07_18.csv`
- `GBPUSD_impossible_candle_2026_07_18.csv`
- `EURUSD_zero_volume_2026_07_18.csv`

#### 2.2 โครงสร้างของไฟล์ CSV
```csv
timestamp,anomaly_type,description,ohlc_details,severity
2026-07-18 10:30:00+00:00,spike,Price spike detected: change 1.5%,OHLC: 1.2345,1.2356,1.2344,1.2355,WARNING
```

### 3. การรวมกับระบบเดิม

ระบบตรวจจับอะโนมัลี้ได้รับการรวมเข้ากับ:
- **DataAdapter:** ตรวจสอบข้อมูลทุกครั้งที่มีการ refresh
- **Configuration:** อยู่ใน `settings.json` ภายใต้ `data_feed.anomaly_detector`
- **Non-blocking:** ไม่ส่งผลต่อการทำงานปกติของบอท

### 4. การตั้งค่าคอนฟิก

```json
{
  "data_feed": {
    "anomaly_detector": {
      "response_time_warning": 7.0,
      "response_time_critical": 10.0,
      "connection_timeout": 15.0,
      "spike_threshold": 0.3,
      "zero_volume_threshold": 0,
      "impossible_candle_threshold": 0.001,
      "spike_window": 20,
      "max_consecutive_zero_volume": 3,
      "max_consecutive_anomalies": 10,
      "enable_csv_logging": true
    }
  }
}
```

## การทดสอบระบบ

### การทดสอบจริง
เพื่อทดสอบระบบใหม่ทั้งหมด ให้รันบอทด้วยคำสั่ง:
```bash
python runner.py
```

ระบบจะทดสอบ:
1. การเชื่อมต่อกับ IQ Option API
2. การตรวจจับอะโนมัลี้ทั้งหมด
3. การบันทึกข้อมูลลง CSV
4. การรวมกับระบบเดิมโดยไม่ส่งผลต่อการซื้อขาย

### การตรวจสอบผลลัพธ์
- ไฟล์ log: `logs/bot.log`
- ไฟล์อะโนมัลี้: `data_base/csv/iq_option/anomaly_logs/`
- สถานะการทำงาน: ตรวจสอบจาก log ของระบบ

## ข้อควรจำ

### Zero Tolerance Policy
- **ไม่มีการ retry:** เมื่อพบข้อผิดพลาด ระบบจะหยุดทันที
- **การบันทึก:** ข้อมูลทั้งหมดถูกบันทึกลง CSV ก่อนหยุด
- **ความปลอดภัย:** การหยุดทันทีช่วยป้องกันการสูญเสียเงินเพิ่มเติม

### การใช้งาน
- ระบบทำงานแบบ non-blocking ไม่ส่งผลต่อการซื้อขายปกติ
- อะโนมัลี้ถูกบันทึกไว้สำหรับการวิเคราะห์หลังการซื้อขาย
- สามารถปรับเปลี่ยน threshold ได้จากไฟล์ settings.json

## การบำรุงรักษา

### การอัปเดต
- อัปเดตค่า threshold ใน `settings.json`
- เพิ่มประเภทของอะโนมัลี้ใหม่ใน code
- ทดสอบระบบหลังการอัปเดต

### การตรวจสอบ
- ตรวจสอบไฟล์ CSV ที่ถูกบันทึก
- ตรวจสอบ log สำหรับข้อผิดพลาด
- ตรวจสอบสถานะของ IQ Option API

## ประวัติการอัปเดต

### v2.0 - Anomaly Detection System
- **วันที่:** 18 กรกฎาคม 2026
- **ฟีเจอร์ใหม่:**
  - ระบบตรวจจับอะโนมัลี้ทั้งหมด
  - Health Check API
  - CSV Logging แบบมาตรฐาน
  - Non-blocking integration
- **การปรับปรุง:**
  - เพิ่มความปลอดภัยในการซื้อขาย
  - ลดความเสี่ยงจากข้อมูลผิดปกติ
  - บันทึกข้อมูลสำหรับการวิเคราะห์

## การสนับสนุน

สำหรับข้อสงสัยหรือปัญหาในการใช้งาน กรุณาตรวจสอบ:
1. ไฟล์ log: `logs/bot.log`
2. ไฟล์ configuration: `settings.json`
3. ไฟล์อะโนมัลี้: `data_base/csv/iq_option/anomaly_logs/`

---

**หมายเหตุ:** ระบบนี้พัฒนาสำหรับการใช้งานใน IQ Option โดยเฉพาะ และใช้ Zero Tolerance Policy เพื่อความปลอดภัยในการซื้อขาย